import { Message } from '../types';

// This file is no longer in use and the mock data has been removed
// as per the user's request. The chat will start fresh each time.
export const MOCK_MESSAGES: Message[] = [];
